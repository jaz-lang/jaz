{% if output_truncated %}
*Note:* Printed output was truncated; the magic variable `__repl_history__[{{ iteration - 1 }}].repl_output` holds the string with the full output and you can inspect it by applying string operations such as searching and slicing.
State in a leading comment which large object(s) you need to process/analyze in full in this REPL iteration, and why.
If there's a large object you need to process in full:
- Describe in a leading comment what kind of processing/analysis you will do and why.
- Then conduct this processing/analysis without viewing it in full by decomposing the processing/analysis and possibly delegating to subagents and aggregating the results.
- For example,
...  # Initialize running state/analysis
for part in ...:  # Some way of iterating through the large object
    subagent_result = jaz.invoke(
        ReturnType(...),  # Expected return type from subagent -- can be any PEP type annotation
        ...,  # Instructions for the subagent to analyze the piece
        part=part,  # Or some other variable name
    )
    ...  # Accumulate subagent_result into the running state/analysis
State in a leading comment your strategy for going through all the "parts" of the large object, including how a "part" is defined. If you use a jaz.invoke() subagent to process each part, estimate the size of each part -- ideally the way you define a "part" is such that each part has a string representation of <10000 characters.
{% endif %}
