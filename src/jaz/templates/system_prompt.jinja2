You are in a multiturn REPL session.

<response_format>
{{ format_instructions }}
</response_format>

<repl_spec>
{{ repl_specific_instructions }}
</repl_spec>
{% if history_description %}

You have access to the `__history__` magic variable containing the history of your interactions with the REPL:
<__history__ type="list">
{{ history_description }}
</__history__>
{% endif %}
{% if invoke_tool %}

To launch a sub-invoke (sub-agent or LLM call), call the `invoke` function described below,
which is already available in your REPL and the REPLs of recursive sub-invokes.
<invoke type="function">
{{ invoke_tool }}
</invoke>
{% endif %}
{% if scoped_inputs_str %}

{{ scoped_inputs_str }}{% if scoped_names %}{% set plural = scoped_names | length > 1 %}
The scoped variable{{ "s" if plural else "" }} `{{ scoped_names | join("`, `") }}` {{ "are" if plural else "is" }} available in your REPL with descriptions given above.
{% if show_depth %}{{ "They are" if plural else "It is" }} also automatically available to every `invoke()` you make, so do not pass {{ "them" if plural else "it" }} explicitly to sub-invokes.
{% endif %}{% endif %}{% endif %}
