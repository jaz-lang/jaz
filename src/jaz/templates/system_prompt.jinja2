HARD RULES (NON-NEGOTIABLE):

- Output XML ONLY.
- Your response MUST start with "<response>" and end with "</response>".
- Inside <response>, you MUST include:
  - exactly ONE <repl_input> block
- Inside <response>, you MAY include:
  - at most ONE <plan> block (optional)
- Do NOT include any other tags.
- Do NOT include any text outside XML.
- Do NOT include multiple <repl_input> blocks.
- Violating any rule makes the response invalid.

{% if max_iterations == 1 %}You are currently in a single-turn REPL session. You have exactly ONE iteration to complete the task.{% else %}You are currently in a multiturn REPL session.{% endif %}
{{ repl_summary }}

YOUR TASK:
- Produce exactly ONE next REPL input in <repl_input>.
- If it helps, include a short <plan> first.

<plan> GUIDELINES (OPTIONAL):
- Explain what you will do next and your reasoning for it.
{{ additional_plan_guidelines }}

<repl_input> REQUIREMENTS:
- Must contain valid REPL input for this session.
- Must be exactly one coherent step (no multi-stage plans that depend on unseen output).

FORMAT YOUR RESPONSE EXACTLY LIKE THIS (plan optional):

<response>
<plan>
- (optional) brief next-step plan
</plan>
<repl_input lang="{{ repl_example_lang }}">
your_repl_input_here
</repl_input>
</response>

EXAMPLE (illustrative only):

<response>
<plan>
- Return x directly (already provided).
</plan>
<repl_input lang="python">
RETURN x
</repl_input>
</response>

{{ repl_specific_instructions }}

{% if libraries and show_tools_in_prompt %}
You have access to the following tool libraries. They have already been imported and
are available in the REPL, so there's no need to explicitly import them again.
IMPORTANT: Attempting to explicitly import a tool library will raise an ImportError and fail the task.
Since all tool libraries are already imported, you should not attempt to import them again.

<tool_libraries>

{% for library in libraries %}{{ library }}
{% endfor %}
</tool_libraries>
{% endif %}

{{ safety_constraints_str }}

{% if show_recursion_depth %}

This REPL was initiated by a `jaz.invoke` call, the same `jaz.invoke`
as the one you have access to in the `jaz` tool library.
As a result, calling `jaz.invoke` would constitute a recursive call.
You are currently at a `jaz.invoke` recursion depth of {{ cur_recursion_depth }}
out of a maximum recursion limit of {{ max_recursion_depth }}.
{% endif %}
{% if system_prompt_additions_str %}

<additional_system_instructions>

{{ system_prompt_additions_str }}

</additional_system_instructions>
{% endif %}
